{
    "name": "@tomgrv/devcontainer-features-gateway",
    "description": "SSL Inspection Gateway handling",
    "version": "7.18.0",
    "bin": {
        "gateway-curl": "stubs/.devcontainer/.gateway/_gateway-curl.sh"
    },
    "dependencies": {},
    "files": [
        "configure-*.sh",
        "install-*.sh",
        "install.sh",
        "stubs/**/*"
    ],
    "peerDependencies": {
        "@tomgrv/devcontainer-features-common-utils": "*"
    },
    "peerDependenciesMeta": {
        "@tomgrv/devcontainer-features-common-utils": {
            "optional": true
        }
    },
    "scripts": {
        "install": "if [ \"$INIT_CWD\" = \"$PWD\" ]; then zz_feature -i -s .; fi",
        "postinstall": "if [ \"$INIT_CWD\" = \"$PWD\" ]; then zz_feature -c -s . gateway; fi"
    }
}
