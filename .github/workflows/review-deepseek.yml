# @format
name: DeepSeek Code Review
on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
  issue_comment:
    types:
      - created
permissions:
  pull-requests: write
  issues: write
jobs:
  deepseek-review:
    if: github.event_name == 'pull_request_target' || contains(github.event.comment.body, '@deepseek')
    timeout-minutes: 30
    runs-on: ubuntu-latest
    name: DeepSeek Code Review
    steps:
      - name: Acknowledge comment
        if: github.event_name == 'issue_comment'
        uses: tomgrv/actions/confirm-pr-comment@v2
        with:
          reaction: eyes

      - name: DeepSeek Code Review
        uses: hustcer/deepseek-review@v1
        with:
          chat-token: ${{ secrets.DEEPSEEK_CHAT_TOKEN }}
          watch-mention: '@deepseek'
          github-token: ${{ github.token }}
