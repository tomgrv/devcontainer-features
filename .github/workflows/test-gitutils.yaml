# @format

name: Test gitutils scripts

on:
  push:
    paths:
      - 'src/gitutils/**'
  pull_request:
    paths:
      - 'src/gitutils/**'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Bats
        uses: bats-core/bats-action@4.0.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run gitutils tests
        run: bats src/gitutils/tests/
